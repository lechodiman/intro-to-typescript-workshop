import { HasTail, HasWings, HasLegs } from './1-basics';

/* FUNCTIONS */

// function addNumbers(num1: number, num2: number) {
//   return num1 + num2;
// }

// Equivalent but more verbose
// function addNumbers(num1: number, num2: number): number {
//   return num1 + num2;
// }

// function makeItFly(animal: HasWings) {
//   console.log(
//     `${animal.name} is flying with wings of type: ${animal.wingType}`
//   );
// }

// const makeItFly = (animal: HasWings) => {
//   `${animal.name} is flying with wings of type: ${animal.wingType}`;
// };
